# Core ML Libraries (minimal builds)
tensorflow-cpu==2.15.0
scikit-learn==1.0.2
lightgbm==4.1.0  # Lighter alternative to XGBoost
numpy==1.23.5
pandas==1.3.4

# Web Framework (minimal)
flask==2.0.1
werkzeug==2.0.3  # Required by Flask
gunicorn==21.2.0

# Utilities (minimal)
pyyaml==6.0
joblib==1.1.0

# Pin versions to avoid pulling in unnecessary dependencies
setuptools==65.5.1
wheel==0.38.4